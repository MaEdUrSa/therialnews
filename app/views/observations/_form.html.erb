<%= form_with(model: observation) do |form| %>
  <% if observation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(observation.errors.count, "error") %> prohibited this observation from being saved:</h2>

      <ul>
        <% observation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="container"> 
      <div>
        <%= form.label :content, style: "display: block" %>
        <%= form.text_area :content, class:"mb-3"  %>
      </div>

      <div>
        <%#= form.label :user_id, style: "display: block" %>
        <%#= form.text_field :user_id %>
      </div>

      <div>
        <%= form.label :post_id, style: "display: block" %>
        <%= form.text_field :post_id, class:"mb-3" %>
      </div>

      <div>
        <%= form.submit class:"btn btn-dark"  %>
        <%= link_to "Volver a post", posts_path, class:"btn btn-dark"%>
      </div>
    <% end %>

</div>